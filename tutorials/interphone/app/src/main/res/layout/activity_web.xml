<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <include
            layout="@layout/tool_bar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

        <ProgressBar
            android:id="@+id/progressbar"
            style="@android:style/Widget.ProgressBar.Horizontal"
            android:layout_width="match_parent"
            android:layout_height="@dimen/webview_progress_bar_height"
            android:max="100"
            android:progress="0"
            android:visibility="gone"/>

        <WebView
            android:id="@+id/webview"
            android:layout_width="match_parent"
            android:layout_height="match_parent"/>

        <!-- 圏外時表示 -->
        <RelativeLayout
            android:id="@+id/webview_network_error_layout"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:visibility="gone">

            <ImageView
                android:id="@+id/webview_network_error_icon"
                android:layout_width="@dimen/webview_network_error_icon_width"
                android:layout_height="@dimen/webview_network_error_icon_height"
                android:layout_centerHorizontal="true"
                android:src="@drawable/icon_network_error" />

            <TextView
                android:id="@+id/webview_error_text"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/margin_vertical_16dp"
                android:layout_below="@+id/webview_network_error_icon"
                style="@style/WebviewNetworkError"
                android:text="@string/webview_netowork_error" />

        </RelativeLayout>

    </LinearLayout>
</FrameLayout>